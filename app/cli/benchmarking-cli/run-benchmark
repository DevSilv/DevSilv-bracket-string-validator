#!/bin/bash

ARG="$1"

if [ -f "$ARG" ] ; then
    node "./app/logic/benchmarking/non-testable/run-benchmark.js" "$ARG"
elif [ "$ARG" == "" ] ; then
    node "./app/logic/benchmarking/non-testable/run-benchmark.js"
elif [ -e "$ARG" ] ; then
    printf "\nFile of wrong type specified: $ARG. You may use only regular files.\n"
    exit 1
else
    printf "\nThe specified file does not exist: $ARG.\n"
    exit 1
fi